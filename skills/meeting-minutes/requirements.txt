# meeting-minutes Python deps. Install: pip install -r requirements.txt
# --- Required (dry_run.py config validation) ---
PyYAML>=6.0
# --- Recommended (phase 1/3 source parsing) ---
python-pptx>=0.6.21   # 회의자료 슬라이드 텍스트 추출 (markitdown 한글깨짐 회피)
openpyxl>=3.1         # 식별자 source-of-truth xlsx 교차검증
# --- Optional (heavy; only if you feed audio recordings) ---
# openai-whisper>=20231117   # 오디오 STT. 설치 큼(torch) — 텍스트/PDF만 쓰면 불필요. 주석 해제 시 사용.
