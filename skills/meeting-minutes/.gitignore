# Personal/private — never commit (this skill dir is pushed to a public GitHub remote).
config.yaml
# Any profile except the shipped examples is private by default.
profiles/*
!profiles/_template/
!profiles/example-acme/

# Publish-protocol run state (snapshots, rendered bodies, event log). Normally created
# next to the work MD, but ignored here too for orgs whose work folder is inside a repo.
.mm/

# Backups / scratch
*.bak
verify-denylist.local
