# Structure (profile: _template)

> 회의의 **형태**를 정의 — 섹션 순서·카테고리·Action 그룹·제목 규칙·타입별 작성 규칙.
> 엔진은 형태를 강제하지 않는다. 이 파일이 있으면 그대로 쓰고, 없으면(profile=null) 녹취서 추론 후 확인.
> **조직·회의 종류마다 완전히 다름** — 운영회의/디자인리뷰/1:1/세미나는 섹션도 Action 방식도 다 다르다.

## 회의 카테고리
<!-- 이 조직이 실제로 하는 회의 종류. config.categories 키와 연결. 예: daily / regular / 1on1 / design-review -->
-

### 판별 신호 (discriminator — 채널 오발송 방지)
<!-- 녹취만 보고 카테고리를 구분할 신호. 카테고리별 1줄: 길이·주기·참석자·주제 패턴.
  pipeline phase 4가 이 섹션으로 판별 — 신호 모호하면 사용자 확인 후 산출물 생성. -->
- (카테고리): (신호)
- **모호하면 추측 금지 → 사용자에게 카테고리 확인.** 폴더/파일명 힌트도 참조.

## 카테고리별 섹션 순서
<!-- 각 카테고리의 본문 섹션을 어떤 순서로. 예:
  daily: 개요 → 이전 회의 연계 → 안건별 논의 → Action Items
  design-review: 목표 → 제안안 → 피드백 → 결정/보류 → 후속
  1on1: 근황 → 논의 주제 → 합의/액션 (Action 그룹화 없음) -->
-

## Action Items 그룹 방식
<!-- 어떻게 묶나. 예: 조직별(다조직 컨소시엄) / 담당자별 / 없음(개인 회의) / 우선순위별 -->
-

## 안건 제목 규칙
<!-- 예: 증상 기반·정량 마커(운영회의) / 안건명 그대로(기획회의) / 주제 키워드(세미나) -->
-

## 타입별 작성 규칙 (선택)
<!-- 이 회의 타입에만 적용되는 특수 규칙. 예:
  - 크로스조직 데이터 요청엔 실데이터(식별자·타임스탬프) 박기 (운영회의)
  - 리포트와 동반 시 리포트 수치 중복 배제, 해석만 (정기 운영회의)
  - 디자인 결정은 근거+대안+트레이드오프 명시 (디자인리뷰) -->
-

## 인라인 주석 마커 (선택 — `(*...)`)
<!-- 사용자(또는 STT 교정 스킬)가 녹취록 원문에 남긴 사후 코멘트를 회의록 섹션으로 승격하는 규칙.
  문법: `(*키워드_짧은설명)`. `*` 뒤 공백 허용. 안 쓰면 이 섹션 통째로 비워둠.
  - 2종 구분: 내용형(괄호 안이 페이로드) vs 태그형(붙은 발언이 페이로드 → (마커, host 발언) 쌍으로 추출)
  - 키워드 → 섹션 라우팅 표를 아래에 정의 (기존 골격 섹션에만 — 신규 섹션 신설 금지)
  - 미분류 마커는 초안 하단 `### 미분류 마커` 블록에 노출 (silent drop 금지)
  - 담당자 접미 규칙(예: `이름+직급약어`)이 있으면 contacts.md와 매핑
  - 사용자 사후 주석 ≠ 회의 발언 — 발언처럼 렌더 금지 (검토 주석 provenance 표기)
  - 외부 산출물(공유본)엔 raw `(*...)` 미포함 -->
| 마커 키워드 | 승격 위치 |
|---|---|
| (예: `to-do`) | (예: Action Items) |
